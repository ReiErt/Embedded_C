Task: Using hardware timers, create a software timer menu on terminal that you can manipulate via UART:

The timer menu has the following features:

1. Start the software timer
2. Stop the software timer. Print the stopped time on the terminal.
3. Input the timer duration.
4. Print currenty timer duration.

The input timer duration will be saved in EEPROM, instead of flash.

